Add a dataset to this folder and configure the constant DATASET_TO_TRAIN with the name of the folder with dataset.
The dataset should contain images sorted by folder that represents different document classes.
